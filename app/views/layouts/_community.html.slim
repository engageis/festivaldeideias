h3 Comunidade
h4
  | Quem já faz parte
.community
   ul
    - if not @users.nil?
      - @users.each do |user|
        li
          - if not user == current_user
            - profile = user.services.first.facebook_profile
            = link_to (image_tag user.services.first.facebook_avatar, :alt => user.name, :title => user.name), profile[:url], :target => profile[:target]

